# Riman HR — Environment Configuration
# Copy to .env and set your values before running.

# Database (SQLite for portable standalone builds)
DATABASE_URL=file:./dev.db

# Authentication (generate a random secret: openssl rand -hex 32)
AUTH_SECRET=REPLACE_WITH_RANDOM_SECRET_AT_LEAST_32_CHARS
AUTH_URL=http://localhost:3000
